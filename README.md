# Travels
Travel Web Portal description: User auth: registration + login with hashed passwords (BCrypt) Package search/browse: search by destination, view details Booking flow: choose date + travelers → simulated payment → confirmation My bookings page for users Admin panel: manage packages (add/delete), view all bookings, view all users Clean HTML/CSS front
